# คู่มือเปิดใช้งานไมโลกับ LINE

ไมโลเป็นผู้ช่วยภาษาไทยสำหรับ LINE ที่รับข้อความส่วนตัวและกลุ่มเพื่อจัดการการเตือน คลังไฟล์ โน้ต งาน รายรับรายจ่าย และการอ่านข้อมูลจากใบเสร็จหรือสลิป โดย dashboard จะแสดงข้อมูลของบัญชี LINE ที่เชื่อมอยู่เท่านั้น

## การตั้งค่า LINE Messaging API

เริ่มจากสร้าง **LINE Messaging API channel** ใน LINE Developers Console แล้วเก็บ Channel access token และ Channel secret ไว้ใน secrets ของโครงการตามชื่อตัวแปร `LINE_CHANNEL_ACCESS_TOKEN` และ `LINE_CHANNEL_SECRET` เท่านั้น ไม่ควรนำค่าเหล่านี้ไปใส่ใน source code หรือส่งผ่านแชท

เมื่อเว็บไซต์ production พร้อมใช้งาน ให้ตั้ง Webhook URL เป็น `https://<โดเมน-production>/api/line/webhook` แล้วกด **Verify** และเปิด **Use webhook** หากต้องการใช้ในกลุ่ม ให้เปิดสิทธิ์การเข้ากลุ่มของบอตใน LINE Developers Console จากนั้นเพิ่ม Official Account ของไมโลเป็นเพื่อนและส่งคำว่า `ช่วย` ในแชทส่วนตัวเพื่อตรวจคำสั่งที่รองรับ

| ตรวจสอบ | ผลลัพธ์ที่ควรเห็น |
|---|---|
| Verify webhook | LINE Developers Console แจ้งว่าสำเร็จ |
| ส่ง `ช่วย` | ไมโลตอบรายการคำสั่งภาษาไทย |
| เพิ่มไมโลในกลุ่มและส่งข้อความ | กลุ่มปรากฏใน dashboard เมื่อ webhook ได้รับ event |
| ส่งไฟล์/ลิงก์ | รายการปรากฏในคลังของ dashboard |

## เชื่อม dashboard กับบัญชี LINE

ลงชื่อเข้าใช้ dashboard ด้วยบัญชีเจ้าของโครงการ แล้วส่งคำว่า `ไอดี` หาไมโลใน **แชทส่วนตัว** ไมโลจะตอบ LINE User ID ของบัญชีดังกล่าว ให้นำรหัสนั้นมาวางในฟอร์มเชื่อมบัญชีบน dashboard

> LINE User ID ที่ dashboard รับได้ต้องขึ้นต้นด้วย `U` และตามด้วยอักขระ hexadecimal 32 ตัว หากเชื่อมผิด ให้กดไอคอนดินสอข้างสถานะ “เชื่อม LINE แล้ว” เพื่ออัปเดตบัญชี โดยไม่ต้องเปิดเผย token หรือ secret ใด ๆ

หลังเชื่อมสำเร็จ dashboard จะอ่าน reminder, คลังข้อมูล และธุรกรรมของบัญชี LINE นั้นเพื่อแสดงผล กราฟการเงินใช้ข้อมูลธุรกรรมจริงเท่านั้น จึงจะแสดงสถานะว่างอย่างชัดเจนหากไม่มีรายการในช่วงเจ็ดวันล่าสุด

## ตั้งค่าและตรวจ scheduler

ผู้ดูแลระบบสามารถกด **เปิด scheduler** ใน dashboard เพื่อให้ Heartbeat เรียก endpoint `/api/scheduled/reminders` ตามงานที่ตั้งค่าไว้ ระบบมี task หลักและ task กู้คืนสำหรับประมวลผลรายการที่เลยกำหนด พร้อมบันทึกผล delivery เพื่อให้ตรวจสอบย้อนหลังได้ การกดซ้ำเมื่อ task หลักยังทำงานอยู่จะแจ้งสถานะ `already-active` แทนการสร้าง task ซ้ำ

ปุ่ม **ส่งรายการที่ถึงเวลา** มีไว้สำหรับผู้ดูแลเพื่อกู้คืนหรือทดสอบรายการที่เลยกำหนดแล้วเท่านั้น ก่อนใช้งานกับรายการจริงควรตรวจชื่อและเวลาที่แสดงใน dashboard ทุกครั้ง การส่งที่สำเร็จจะบันทึกสถานะ `sent` ใน delivery audit และรายการเตือนครั้งเดียวจะเปลี่ยนเป็น completed

ไมโลตั้งค่าสรุปการเงินอัตโนมัติสำหรับ LINE ส่วนตัวที่เชื่อมไว้แล้ว โดยส่ง **สรุปวันก่อนหน้าเวลา 06:00 น. ทุกวัน** และ **สรุปสัปดาห์ก่อนหน้าเวลา 06:00 น. ทุกวันจันทร์** ตามเวลา Asia/Bangkok การส่งแต่ละรอบใช้ delivery audit แบบ idempotent จึงไม่ส่งซ้ำเมื่อ Heartbeat เรียกซ้ำ รายงานจะใช้ข้อมูลธุรกรรมจริงที่อยู่ในรอบที่ปิดแล้วเท่านั้น และ fallback เป็นข้อความเมื่อ LINE ไม่รับ Flex card

## คำสั่งตัวอย่างใน LINE

| ความต้องการ | ตัวอย่าง |
|---|---|
| เตือนครั้งเดียว | `เตือนประชุมทีมพรุ่งนี้ 10:30` |
| เตือนซ้ำ | `เตือนดื่มน้ำทุก 30 นาที` |
| เก็บลิงก์พร้อมแท็ก | `เก็บ https://example.com/brief #งาน #สำคัญ` |
| โน้ตและงาน | `โน้ต รหัสประตู 1234` หรือ `งาน ส่งสรุปก่อนศุกร์` |
| รายรับรายจ่าย | `จ่ายกาแฟ 65 บาท` หรือ `รับเงินเดือน 35000 บาท` |
| หมวดมาตรฐาน | รายจ่ายรองรับ อาหาร, เดินทาง, ที่อยู่อาศัย, ค่าสาธารณูปโภค, สุขภาพ, ประกัน, หนี้สิน และอื่น ๆ; รายรับรองรับ เงินเดือน, งานอิสระ, ขายสินค้า/บริการ, ดอกเบี้ย/เงินปันผล และเงินคืน |
| ส่งเสียงเป็นรายการ | ส่งข้อความเสียง เช่น “จ่ายกาแฟ 65 บาท” แล้วตรวจข้อความถอดเสียงก่อนกด **ยืนยันบันทึก**; หากไม่มีปุ่ม ให้พิมพ์ `ยืนยันเสียง` |
| เปลี่ยนหมวดข้อเสนอเสียง | กด **แก้ไขข้อความ** แล้วเลือกหมวด Quick Reply เช่น `อาหาร` หรือ `เดินทาง`; ระบบจะเสนอรายการใหม่และยังไม่บันทึกจนกดยืนยัน |
| แก้ไขข้อความเสียงทั้งหมด | พิมพ์ `แก้ไขเสียง จ่ายกาแฟ 65 บาท` แล้วตรวจข้อเสนอใหม่ก่อนยืนยัน |
| ดูยอดทันทีหลังบันทึก | กดปุ่ม **ดูสรุปยอดวันนี้** บนการ์ดยืนยัน หรือพิมพ์ `สรุปวันนี้` |
| ค้นหารายการย้อนหลัง | `ค้นหารายการ กาแฟ` |
| แก้ไขยอดธุรกรรม | `แก้รายการ 12 เป็น 180` |
| ลบธุรกรรม | `ลบรายการ 12` — ระบบลบแบบ soft-delete และเก็บ audit log |
| รายงานตามรอบ | `สรุปวันนี้`, `สรุปสัปดาห์นี้`, `สรุปเดือนนี้` หรือ `สรุปปีนี้` |
| AI วิเคราะห์ | `วิเคราะห์รายจ่าย` หรือ `สรุปธุรกิจ` เพื่อสรุปจากรายการจริงของเดือนปัจจุบัน |
| งบประมาณ | `ตั้งงบ อาหาร 5000 บาท` |
| แจ้งเพื่อนในกลุ่ม | `แจ้งส่งรายงานด้วยถึง @สมชาย` |
| ขอรหัสสำหรับเชื่อม dashboard | `ไอดี` ในแชทส่วนตัวกับไมโล |
| วิเคราะห์รูป | ส่งใบนัด ใบเสร็จ หรือสลิป แล้วตรวจข้อเสนอของไมโลก่อนยืนยัน |

## การอ่านใบเสร็จและสลิป

ไมโลจะสกัดร้านค้า จำนวนเงิน วันที่ หมวด วิธีชำระ เลขอ้างอิง และรายการสำคัญจากภาพ แล้วเสนอข้อมูลให้ตรวจสอบก่อนบันทึกเป็นธุรกรรม การยืนยันจะไม่เกิดขึ้นหากจำนวนเงินหรือวันที่อ่านไม่ชัด เพื่อป้องกันข้อมูลที่เดาเอง

หากภาพไม่มีวันที่ชัดเจน ให้ระบุวันด้วยคำสั่ง `ยืนยันค่าใช้จ่าย วันที่ DD/MM/YYYY` แล้วตรวจข้อความสรุปอีกครั้งก่อนบันทึก การบันทึกรายการดังกล่าวจะอัปเดตบัตรสรุป กราฟกระแสเงินเข้า–ออกเจ็ดวัน และกราฟสัดส่วนหมวดรายจ่ายใน dashboard โดยอัตโนมัติ

หลังบันทึกรายรับ รายจ่ายจากข้อความ เสียง หรือรูปที่ยืนยันแล้ว ไมโลจะแสดงการ์ดสรุปที่ใช้ **ยอดจริงของวันนั้น** พร้อมปุ่มดูสรุปยอดวันนี้ หาก LINE ไม่รับ Flex Message ระบบจะส่งข้อความสำรองพร้อม Quick Reply ปุ่มเดียวกัน โดยการ์ดจะไม่แสดงยอดรวมก่อนที่ระบบจะคำนวณรายงานรายวันเสร็จ

การ์ดสรุปรายวัน รายสัปดาห์ และรายเดือนมีแถบกราฟิก `MILO • FINANCE` พร้อมน้องแมวในโทนม่วง–ชมพู–มิ้นท์ ผู้ใช้สามารถพิมพ์ `สรุปสัปดาห์นี้` เพื่อดูตัวอย่างการ์ดที่คำนวณจากข้อมูลจริง โดยคำสั่งนี้ไม่สร้างธุรกรรมใหม่

## รายงาน ส่งออก และผู้ดูแล

Dashboard รองรับรายงานตามวัน สัปดาห์ เดือน ปี และช่วงวันที่กำหนดเอง โดยคำนวณจากธุรกรรมที่ยัง active เท่านั้น สามารถดาวน์โหลด CSV หรือ Excel (`.xlsx`) ซึ่งมีชีตธุรกรรม สรุป และรายจ่ายตามหมวดจากข้อมูลจริง

ในฐานะผู้ดูแล สามารถกำหนดบทบาท `viewer`, `user`, `manager` และ `admin` ให้บัญชี dashboard ได้ พร้อมดู audit log ของการเชื่อม LINE การสร้าง แก้ไข ลบธุรกรรม และการเปลี่ยนสิทธิ์ การลบธุรกรรมเป็น soft-delete เพื่อให้ประวัติยังตรวจสอบย้อนหลังได้

> AI ใช้เพื่อสรุปข้อมูลและช่วยจัดหมวดเบื้องต้นเท่านั้น คำตอบจะยึดข้อมูลธุรกรรมที่บันทึกไว้ และจะแสดงว่าข้อมูลไม่เพียงพอเมื่อยังมีรายการน้อย ไม่ควรใช้เป็นคำแนะนำลงทุน ภาษี หรือการตัดสินใจทางการเงินสำคัญ

## แนวทางใช้งานอย่างปลอดภัย

ควรทดสอบการกล่าวถึงสมาชิกในกลุ่มที่ไม่ใช่ข้อมูลสำคัญก่อน โดยสมาชิกที่ต้องการ mention ควรส่งข้อความในกลุ่มอย่างน้อยหนึ่งครั้งเพื่อให้ไมโลจดจำบริบทได้ สำหรับ reminder สำคัญ ให้ตรวจ timezone และเวลาใน dashboard ก่อนบันทึก และอย่าลบหรือแก้ไขรายการจริงเพื่อใช้ทดสอบระบบ

หากไมโลไม่ส่งการเตือน ให้ตรวจตามลำดับดังนี้: การเชื่อมบัญชี LINE ถูกต้อง, task scheduler อยู่ในสถานะ active, reminder ยังเป็น `active` และถึงเวลาแล้ว, จากนั้นให้ผู้ดูแลตรวจผล delivery audit หรือใช้ปุ่มส่งรายการที่ถึงเวลาเฉพาะเมื่อทราบว่าเป็นรายการที่ควรประมวลผลจริง
